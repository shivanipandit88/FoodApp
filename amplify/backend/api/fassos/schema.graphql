type Restaurant @model {
  id: ID!
  name: String!
  description: String
  image: String
  username: String
}

type AddMenu @model {
  id: ID!
  dishname: String!
  ingredients: String!
  image: String
  price: Int!
  dishid: ID!
  restaurant: Restaurant @connection(fields: ["dishid"])
}
